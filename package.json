{
  "name": "chrome-vk-comment-blocker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "rimraf extension/scripts && cd scripts && tsc"
  },
  "devDependencies": {
    "rimraf": "^2.5.3",
    "typescript": "^1.8.10"
  }
}
